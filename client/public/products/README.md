# Product photos (local only)

Real photographs of handmade crochet inventory. **Not tracked in Git** — kept on your machine for local seeding and development.

Production uses Cloudinary URLs stored in MongoDB.

To seed locally, place image files here and run `npm run seed` from `server/`.
